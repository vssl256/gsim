import javafx.scene.paint.Color;

public class Atmosphere {
    public double radius;
    public double opacity;
    public Color color;

    public Atmosphere(double radius, double opacity, Color color) {
        this.radius = radius;
        this.opacity = opacity;
        this.color = color;
    }
}
